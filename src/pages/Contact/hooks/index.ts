/**
 * Contact hooks module exports
 *
 * Centralizes all React hooks related to contact functionality
 * and re-exports relevant types for convenient imports.
 */

export { useContactForm } from './useContactForm'
export type { ContactFormData, SubmitFormOptions } from '../types'
